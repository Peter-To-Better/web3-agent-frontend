# Real backend base URL, e.g. http://backend-service:8080 or an internal
# EC2/ALB address. Leave empty to use the built-in mock chat responses.
BACKEND_URL=

# Paths appended to BACKEND_URL by the SSE/REST proxy routes.
BACKEND_CHAT_STREAM_PATH=/chat/stream
BACKEND_CHAT_HISTORY_PATH=/chat/history
